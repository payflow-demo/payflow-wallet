# Layer 2 (Helm addons) is installed by the bootstrap-node instance user_data
# after cluster and nodes are ACTIVE. No kubernetes/helm providers or resources here.
# See: modules/bootstrap-node/templates/bootstrap.sh
