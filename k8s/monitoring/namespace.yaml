# ============================================
# Monitoring Namespace
# ============================================
# Purpose: Separate namespace for monitoring stack (Prometheus, Grafana, AlertManager)
# This keeps monitoring resources isolated from application resources
apiVersion: v1
kind: Namespace
metadata:
  name: monitoring
  labels:
    name: monitoring
    purpose: observability

