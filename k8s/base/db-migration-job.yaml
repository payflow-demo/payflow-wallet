apiVersion: batch/v1
kind: Job
metadata:
  name: db-migration-job
  namespace: payflow
  annotations:
    # Run after External Secrets Operator has synced db-secrets (ArgoCD sync-wave).
    # Ensures secret exists before pod is created; otherwise job pod stays Pending and backoffLimit can be exhausted.
    argocd.argoproj.io/sync-wave: "2"
spec:
  backoffLimit: 3
  ttlSecondsAfterFinished: 3600  # Clean up after 1 hour
  template:
    spec:
      restartPolicy: OnFailure
      containers:
      - name: db-migration
        image: postgres:16-alpine
        securityContext:
          allowPrivilegeEscalation: false
        resources:
          requests:
            cpu: "100m"
            memory: "128Mi"
          limits:
            cpu: "500m"
            memory: "256Mi"
        command: ['sh', '-c']
        args:
        - |
          set -e
          echo "Waiting for PostgreSQL to be ready..."
          
          # Wait for database to be ready
          until pg_isready -h ${DB_HOST} -p ${DB_PORT} -U ${DB_USER}; do
            echo "PostgreSQL not ready, waiting..."
            sleep 5
          done
          
          echo "PostgreSQL is ready! Running migrations..."
          
          # Run Flyway migrations if available, otherwise run SQL files
          if [ -d "/migrations" ] && [ "$(ls -A /migrations/*.sql 2>/dev/null)" ]; then
            echo "Running migrations from /migrations directory..."
            for migration in /migrations/*.sql; do
              echo "Executing: $migration"
              psql -h ${DB_HOST} -p ${DB_PORT} -U ${DB_USER} -d ${DB_NAME} -f "$migration"
            done
          else
            echo "No migration files found in /migrations, running inline migrations..."
            
            # Run V1__initial_schema.sql
            psql -h ${DB_HOST} -p ${DB_PORT} -U ${DB_USER} -d ${DB_NAME} <<EOF
            $(cat <<'EOSQL'
            -- V1: Initial Schema
            CREATE TABLE IF NOT EXISTS users (
              id VARCHAR(50) PRIMARY KEY,
              email VARCHAR(255) UNIQUE NOT NULL,
              password_hash VARCHAR(255) NOT NULL,
              name VARCHAR(100) NOT NULL,
              role VARCHAR(20) DEFAULT 'user',
              is_active BOOLEAN DEFAULT true,
              email_verified BOOLEAN DEFAULT false,
              two_factor_enabled BOOLEAN DEFAULT false,
              two_factor_secret VARCHAR(255),
              failed_login_attempts INT DEFAULT 0,
              locked_until TIMESTAMP,
              created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
              updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
              last_login_at TIMESTAMP,
              last_login_ip VARCHAR(45)
            );
            
            CREATE TABLE IF NOT EXISTS wallets (
              user_id VARCHAR(50) PRIMARY KEY REFERENCES users(id) ON DELETE CASCADE,
              name VARCHAR(100) NOT NULL,
              balance DECIMAL(15,2) NOT NULL DEFAULT 0.00,
              currency VARCHAR(3) DEFAULT 'USD',
              created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
              updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
            );
            
            CREATE TABLE IF NOT EXISTS transactions (
              id VARCHAR(50) PRIMARY KEY,
              from_user_id VARCHAR(50) NOT NULL,
              to_user_id VARCHAR(50) NOT NULL,
              amount DECIMAL(15,2) NOT NULL,
              status VARCHAR(20) NOT NULL,
              error_message TEXT,
              created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
              processing_started_at TIMESTAMP,
              completed_at TIMESTAMP,
              CONSTRAINT fk_from_user FOREIGN KEY (from_user_id) REFERENCES users(id),
              CONSTRAINT fk_to_user FOREIGN KEY (to_user_id) REFERENCES users(id)
            );
            
            CREATE TABLE IF NOT EXISTS notifications (
              id SERIAL PRIMARY KEY,
              user_id VARCHAR(50) NOT NULL REFERENCES users(id) ON DELETE CASCADE,
              type VARCHAR(50) NOT NULL,
              message TEXT NOT NULL,
              transaction_id VARCHAR(50),
              read BOOLEAN DEFAULT FALSE,
              created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
            );
            
            CREATE TABLE IF NOT EXISTS refresh_tokens (
              id SERIAL PRIMARY KEY,
              user_id VARCHAR(50) REFERENCES users(id) ON DELETE CASCADE,
              token VARCHAR(500) UNIQUE NOT NULL,
              expires_at TIMESTAMP NOT NULL,
              created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
              ip_address VARCHAR(45),
              user_agent TEXT
            );
            
            CREATE TABLE IF NOT EXISTS user_sessions (
              id SERIAL PRIMARY KEY,
              user_id VARCHAR(50) REFERENCES users(id) ON DELETE CASCADE,
              session_token VARCHAR(500) UNIQUE NOT NULL,
              ip_address VARCHAR(45),
              user_agent TEXT,
              created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
              expires_at TIMESTAMP NOT NULL,
              is_active BOOLEAN DEFAULT true
            );
            EOSQL
            )
            EOF
            
            # Run V2__add_indexes.sql
            psql -h ${DB_HOST} -p ${DB_PORT} -U ${DB_USER} -d ${DB_NAME} <<EOF
            $(cat <<'EOSQL'
            -- V2: Add Indexes
            CREATE INDEX IF NOT EXISTS idx_users_email ON users(email);
            CREATE INDEX IF NOT EXISTS idx_wallets_user_id ON wallets(user_id);
            CREATE INDEX IF NOT EXISTS idx_transactions_from_user ON transactions(from_user_id, created_at DESC);
            CREATE INDEX IF NOT EXISTS idx_transactions_to_user ON transactions(to_user_id, created_at DESC);
            CREATE INDEX IF NOT EXISTS idx_transactions_status ON transactions(status);
            CREATE INDEX IF NOT EXISTS idx_notifications_user ON notifications(user_id, created_at DESC);
            CREATE INDEX IF NOT EXISTS idx_refresh_tokens_user ON refresh_tokens(user_id);
            CREATE INDEX IF NOT EXISTS idx_refresh_tokens_expires ON refresh_tokens(expires_at);
            CREATE INDEX IF NOT EXISTS idx_sessions_user ON user_sessions(user_id);
            CREATE INDEX IF NOT EXISTS idx_sessions_expires ON user_sessions(expires_at);
            EOSQL
            )
            EOF
            
            # Run V3__add_2fa.sql
            psql -h ${DB_HOST} -p ${DB_PORT} -U ${DB_USER} -d ${DB_NAME} <<EOF
            $(cat <<'EOSQL'
            -- V3: 2FA Support (already in V1, but ensuring it exists)
            -- Columns already added in V1, just verify
            DO \$\$ 
            BEGIN
              IF NOT EXISTS (SELECT 1 FROM information_schema.columns 
                            WHERE table_name='users' AND column_name='two_factor_enabled') THEN
                ALTER TABLE users ADD COLUMN two_factor_enabled BOOLEAN DEFAULT false;
                ALTER TABLE users ADD COLUMN two_factor_secret VARCHAR(255);
              END IF;
            END \$\$;
            EOSQL
            )
            EOF
          fi
          
          echo "Database migrations completed successfully"
        env:
        - name: DB_HOST
          valueFrom:
            secretKeyRef:
              name: db-secrets
              key: DB_HOST
        - name: DB_PORT
          valueFrom:
            secretKeyRef:
              name: db-secrets
              key: DB_PORT
        - name: DB_NAME
          valueFrom:
            configMapKeyRef:
              name: app-config
              key: DB_NAME
        - name: DB_USER
          valueFrom:
            secretKeyRef:
              name: db-secrets
              key: DB_USER
        - name: PGPASSWORD
          valueFrom:
            secretKeyRef:
              name: db-secrets
              key: DB_PASSWORD

